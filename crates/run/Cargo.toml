[package]
name = "atomica-run"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
atomica-sim = { workspace = true }

# A headless runner: play a battle and stream the structured combat log.
[[bin]]
name = "atomica-cli"
path = "src/main.rs"
