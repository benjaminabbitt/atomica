[package]
name = "atomica-game"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Macroquad + egui front-end for CHROME AND CODE."

# Fixed bin name so the wasm artifact is always `atomica.wasm` (see web/index.html).
[[bin]]
name = "atomica"
path = "src/main.rs"

[dependencies]
atomica-sim.workspace = true
macroquad.workspace = true
egui-macroquad.workspace = true
